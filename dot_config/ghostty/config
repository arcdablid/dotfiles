# The configuration can be reloaded at runtime by pressing ctrl+shift+,

# The syntax is "key = value". The whitespace around the
# equals doesn't matter.

# background = 282c34
# foreground = ffffff

# Comments start with a `#` and are only valid on their own line.
# Blank lines are ignored!

# keybind = ctrl+z=close_surface
# keybind = ctrl+d=new_split:right

# You can split your configuration into multiple files by using the config-file key in your configuration file.

# config-file = some/relative/sub/config
# config-file = ?optional/config # If the value is prefixed with ?, the file is optional and if it doesn't exist, it is ignored.
# config-file = /absolute/path/config

# ------------------------------------------------------------------------------

theme = Snazzy
background = #1e1f29
foreground = #ebece6
selection-background = #81aec6
selection-foreground = #000000
cursor-color = #e4e4e4
cursor-text = #a9a9a9
cursor-style = bar
palette = 0=#000000
palette = 1=#fc4346
palette = 2=#50fb7c
palette = 3=#f0fb8c
palette = 4=#49baff
palette = 5=#fc4cb4
palette = 6=#8be9fe
palette = 7=#ededec
palette = 8=#555555
palette = 9=#fc4346
palette = 10=#50fb7c
palette = 11=#f0fb8c
palette = 12=#49baff
palette = 13=#fc4cb4
palette = 14=#8be9fe
palette = 15=#ededec
font-family = Hack Nerd Font Mono
mouse-hide-while-typing = true
background-opacity = "0.9"
font-size = "13"
window-height = "48"
window-width = "128"
